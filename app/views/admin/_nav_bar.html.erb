<nav class="navbar navbar-expand-lg mx-3">
  <div class="container-fluid">
    <a><%= link_to t('.project'), root_path, class: "navbar-brand mb-0 h1" %></a>

    <span class="navbar-brand"><%= t('.admin_panel') %></span>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page"><%= link_to t('.tests'), admin_tests_path %></a>
        <a class="nav-link">
          <%= link_to t('.gists'), admin_gists_path %>
        </a>
        <a class="nav-link">
          <%= link_to t('.Badges'), admin_badges_path %>
        </a>
      </div>
    </div>

    <span>
      <%= t('.hello_message', surname: current_user.surname, name: current_user.name) %>
      <%= link_to t('.sign_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-secondary text-light" %>
    </span>
  </div>
</nav>