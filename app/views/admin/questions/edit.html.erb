<h1><%= t('.edit_question') %></h1>

<%= render 'form' %>

<%= link_to t('.back_to_question'), admin_question_path(@question) %>
<%= link_to t('.back_to_test'), admin_test_path(id: @question.test_id) %>
