<p>
  <strong><%= t('.question') %></strong>
  <%= @question.body %>
</p>

<h2><%= t('.header') %></h2>

<%= link_to t('.new_answer'), new_admin_question_answer_path(@question), class: "btn btn-info" %>
<br>
<br>

<table>
  <%= render @question.answers %>
</table>

<br>
<%= link_to t('.edit'), edit_admin_question_path(@question) %>
<%= link_to t('.back_to_test'), admin_test_path(id: @question.test_id) %>
